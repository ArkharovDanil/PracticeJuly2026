<Window x:Class="MyWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyWpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <Grid Grid.Row="0" Grid.ColumnSpan="2" Background="LightGray" Margin="5">
            <Button x:Name="GenerateButton" Content="Сгенерировать" 
                    Width="120" Height="30" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Click="GenerateButton_Click"/>
        </Grid>

        <Border Grid.Row="1" Grid.Column="0" BorderBrush="Black" BorderThickness="2" Margin="5">
            <Grid>
                <TextBlock Text="Зеленые линии" 
                           VerticalAlignment="Top" HorizontalAlignment="Center" 
                           FontWeight="Bold" Margin="5"/>
                <Canvas x:Name="LeftCanvas" Background="White" Margin="0,30,0,0"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" BorderBrush="Black" BorderThickness="2" Margin="5">
            <Grid>
                <TextBlock Text="Черные пунктирные линии" 
                           VerticalAlignment="Top" HorizontalAlignment="Center" 
                           FontWeight="Bold" Margin="5"/>
                <Canvas x:Name="RightCanvas" Background="White" Margin="0,30,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>